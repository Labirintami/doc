---
sidebar_label: validate
title: validate
---

import { Short, Descr } from '@site/src/components';

          


<Short>
 validates a Combo control
 

</Short>



- silent    boolean     optional, if true - the method will return the result of validation without calling validation events and without modifying the control visually
- validateValue     string|string[]     optional, the value to be validated. If not specified, the method validates the current value of the control


param   boolean     true - if a control is valid, otherwise false

~~~js

// the method validates the specified value
form.getItem("combo").validate(true, "id_2"); // -> true/false

// the method validates the current value
form.getItem("combo").validate(true); // -> true/false

~~~
 api_method

<Descr>

When calling without parameters or setting the <strong>silent</strong> parameter to *false*, the method invokes the [BeforeValidate](/form/api/combo/combo_beforevalidate_event) and [AfterValidate](/form/api/combo/combo_aftervalidate_event) events and visually modifies the control.

~~~js
// the method validates the specified value
form.getItem("combo").validate(false, "id_2"); // -> true/false

// the method validates the current value
form.getItem("combo").validate(); // -> true/false
~~~



</Descr>


 
The <strong>silent</strong> and <strong>validateValue</strong> parameters are added in v7.0

