---
sidebar_label: save
title: save
---

import { Short, Descr } from '@site/src/components';

          


<Short>

	saves changes made in a tree collection to the server side


</Short>




- url			string		the URL of a server side


~~~js

diagram.data.save("http://userurl/");


~~~
	api_method

<Descr>


The component will make an AJAX call and expect the remote URL to save data changes.

Data saving is asynchronous, so you need to return a promise - the result of the saving operation. To do this, use the <strong>saveData</strong> property that returns a "promise" object:

~~~js
var data = new TreeCollection();
data.save(loader);
return data.saveData.then(function () {
    // now your data is saved
});
~~~

Use the [isSaved](/tree_collection/api/issaved) method to know whether the changes are saved:

~~~js
diagram.data.saveData.then(function () {
	console.log(diagram.data.isSaved());
});
~~~



</Descr>

